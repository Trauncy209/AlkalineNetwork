# Alkaline Network - Python Dependencies
# Install with: pip install -r requirements.txt

# Core - Required
pynacl>=1.5.0          # Encryption (X25519 + XSalsa20-Poly1305)
requests>=2.28.0       # HTTP requests
paramiko>=3.0.0        # SSH for device provisioning

# Payments - Required for production
stripe>=5.0.0          # Payment processing and payouts

# Optional
aiohttp>=3.8.0         # Async HTTP (for mesh discovery)
